import React from 'react'
import { BrowserRouter as Router, Route, Link } from "react-router-dom"
function Products() {
    return (
      <div>
          <Link to="/home">this is products</Link>
      </div>
    );
  }
  
export default Products;